<Page
    x:Class="Win2ch.Controls.PostControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win2ch.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Subject}"
                       Visibility="{Binding Subject, Converter={StaticResource StrToVis}}"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource CustomColorBrush}"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Name}"
                           FontWeight="Bold"/>

                <TextBlock
                    Text="{Binding Num, Converter={StaticResource PostN}}"
                    FontWeight="Bold"
                    Foreground="{StaticResource CustomColorBrush}"
                    HorizontalAlignment="Right"
                    Margin="5 0 0 0"
                    Grid.Column="1"
                    Tapped="PostNum_OnTapped"/>
            </Grid>

            <!--<RichTextBlock Grid.Row="3"  TextWrapping="Wrap"
                            local:HtmlToRichTextBlockConverter.Html="{x:Bind Comment}"
                            IsTextSelectionEnabled="False"
                            x:Phase="3"/>-->

            <TextBlock x:Name="PostText"
                       Grid.Row="3"
                       Margin="0 5"
                       Text="{Binding Text}"
                       IsTextSelectionEnabled="True"/>

            <TextBlock Grid.Row="4"
                       Opacity="0.7"
                       Text="{Binding Date}"
                       FontSize="13"/>
        </Grid>
    </Grid>
</Page>
