<UserControl
    x:Class="Win2ch.Controls.PostControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win2ch.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    xmlns:m="using:Win2ch.Models">

    <Grid Padding="5" Background="{ThemeResource PostBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 5">
            <TextBlock Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Bottom"/>
            <TextBlock Text="{Binding Date}" Margin="5 0 0 0" VerticalAlignment="Bottom"/>
            <TextBlock
                    Text="{Binding Num}" Margin="5 0 0 0" FontWeight="Bold"
                    Foreground="{StaticResource CustomColorBrush}"
                    VerticalAlignment="Bottom"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding Comment}" TextWrapping="Wrap"/>
    </Grid>

</UserControl>
