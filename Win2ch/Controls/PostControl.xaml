<Page
    x:Class="Win2ch.Controls.PostControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win2ch.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="This">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Subject}"
                       Visibility="{Binding Subject, Converter={StaticResource StrToVis}}"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource CustomColorBrush}"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Name}"
                           FontWeight="Bold"/>

                <TextBlock
                    Text="{Binding Num, Converter={StaticResource PostN}}"
                    FontWeight="Bold"
                    Foreground="{StaticResource CustomColorBrush}"
                    Margin="5 0 0 0"
                    Grid.Column="1"
                    Tapped="PostNum_OnTapped"/>
                
                <TextBlock Grid.Column="2"
                           Foreground="{StaticResource CustomColorBrush}"
                           FontWeight="Bold"
                           HorizontalAlignment="Right"/>
            </Grid>

            <GridView Grid.Row="2" 
                      ItemsSource="{Binding Images}"
                      Margin="0 5"
                      Padding="0"
                      SelectionMode="None"
                      IsItemClickEnabled="True"
                      ItemClick="ImagesGridView_OnItemClick">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding ThumbnailUrl}" Height="100" Stretch="Uniform"/>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <TextBlock x:Name="PostText"
                       Grid.Row="3"
                       Text="{Binding Comment}"
                       IsTextSelectionEnabled="{x:Bind IsSimple, Converter={StaticResource InverseBool}}"
                       TextWrapping="Wrap"
                       Visibility="{Binding Comment, Converter={StaticResource StrToVis}}"/>

            <TextBlock Grid.Row="4"
                       Opacity="0.7"
                       Text="{Binding Date}"
                       Margin="0 5 0 0"
                       FontSize="13"/>
        </Grid>
    </Grid>
</Page>
