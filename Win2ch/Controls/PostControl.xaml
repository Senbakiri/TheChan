<UserControl
    x:Class="Win2ch.Controls.PostControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win2ch"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Padding="5" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Subject}" Visibility="{Binding Subject, Converter={StaticResource StrToVis}}"
                        FontWeight="Bold" TextWrapping="Wrap" Foreground="{StaticResource CustomColorBrush}" />
        
        <Grid Margin="0 5" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
            <TextBlock
                    Text="{Binding Num}" Margin="5 0 0 0" FontWeight="Bold"
                    Foreground="{StaticResource CustomColorBrush}"
                    HorizontalAlignment="Right"
                    Grid.Column="2"/>
        </Grid>

        <RichTextBlock Grid.Row="3"  TextWrapping="Wrap" local:HtmlToRichTextBlockConverter.Html="{Binding Comment}" IsTextSelectionEnabled="False"/>

        <TextBlock Grid.Row="4" Opacity="0.7" Text="{Binding Date}" Margin="0 5 0 0" FontSize="13"/>
    </Grid>

</UserControl>
