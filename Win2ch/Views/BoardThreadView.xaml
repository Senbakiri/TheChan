<Page
    x:Class="Win2ch.Views.BoardThreadView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win2ch.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource BoardThreadBackgroundBrush}"
                Margin="10 0 10 10"
                Padding="10">
        <ContentControl x:Name="Post"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"/>
        <TextBlock Margin="0 5 0 0"
                   FontWeight="SemiBold">
            <Run x:Uid="Board/SkippedPosts"/>: <Run Text="{x:Bind ViewModel.ThreadInfo.SkippedPosts}"/>
        </TextBlock>
        <TextBlock Margin="0 5 0 0"
                   FontWeight="SemiBold">
            <Run x:Uid="Board/SkippedPostsWithFiles"/>: <Run Text="{x:Bind ViewModel.ThreadInfo.SkippedPostsWithFiles}"/>
        </TextBlock>
    </StackPanel>
</Page>
