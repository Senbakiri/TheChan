<Page
    x:Class="Win2ch.Views.BoardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win2ch.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView>
            <ListView.Header>
                <TextBlock Foreground="{ThemeResource GrayForegroundBrush}"
                           TextWrapping="Wrap"
                           Padding="10"
                           FontWeight="SemiBold"
                           Text="{x:Bind ViewModel.CurrentPage.BoardName, Converter={StaticResource ToUpper}, Mode=OneWay}"/>
            </ListView.Header>
        </ListView>
    </Grid>
</Page>
